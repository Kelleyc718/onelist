version: '3'

services:
  mongodb:
    image: "mongo:3.2"
    ports:
      - "27017:27017"

  server:
    build: ./server
    ports:
      - "6200:3000"

  client:
    build: ./client
    ports:
      - "3000:3000"
    links:
      - server
    depends_on:
      - server